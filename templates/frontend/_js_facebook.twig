<div class="socialFeeds-facebook-container">
	<div class="socialFeeds-loading"></div>
</div>
{% verbatim %}
	<script type="text/x-handlebars-template" id="socialFeeds-facebook">
		{{#each feed}}
    		<p>{{this.date}}</p>
			<p>{{{this.text}}}</p>
		{{/each}}
	</script>
{% endverbatim %}